
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Dados de estoque (gerados em tempo de execução)
data/*.csv
data/*.csv.bak
data/*.db
data/sync_config.json
__pycache__/
*.pyc

# Artefatos de build do executável (PyInstaller)
build/
dist/
*.spec
